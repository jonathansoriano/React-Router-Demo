name: Deploy React App to GH Pages

on:
  push:
    branches: ['main']

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Code
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: 'npm'
          cache-dependency-path: react-router-demo/package-lock.json

      - name: Install dependencies
        working-directory: ./react-router-demo
        run: npm ci

      - name: Build App
        working-directory: ./react-router-demo
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload Artifacts
        uses: actions/upload-pages-artifact@v5
        with:
          path: './react-router-demo/dist'

      - name: Deploy to GH Pages
        id: deployment
        uses: actions/deploy-pages@v5
